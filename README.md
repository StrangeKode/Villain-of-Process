# VOP (Villain Of Process)

Mini administrador de tareas estilo `htop`, hecho en **C++** con `ncurses`.

## 🚀 Compilación

```bash
git clone https://github.com/TU-USUARIO/VOP.git
cd VOP
mkdir build && cd build
cmake ..
make
./vop
