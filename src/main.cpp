#include "Ui.h"

/*
 * Función principal del programa.
 * Propósito: Crear la interfaz de usuario y ejecutar el monitor de procesos.
 */
int main() {
    Ui ui;
    ui.run();
    return 0;
}